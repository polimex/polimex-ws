# Polimex Holding Ltd. - https://polimex.co
# P0 scaffold: no models yet. P1 adds polimex.ws.mixin (shared transport+auth),
# P2 adds polimex.ws.endpoint (one credential row per serial) + the session mixin.
